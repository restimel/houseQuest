liste BUgs && todo

*II* display
    → do not display existing result
    → watch similar result
*III* filter results by different criteria / paginate other results
*III* previous defaultInfo selection not displayed at start
*III* Keep results in memory (when switching tabs)
*IV* performance optimization → quick search (no A*)
*IV* performance optimization → check start and check end (sortcut house)
